spring:
  config:
    activate:
      on-profile: test
    import:
      - application-mybatis.yml
      - application-sql.yml
      - security/application-datasource.yml
      - security/application-security.yml
